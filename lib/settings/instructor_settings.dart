import 'package:flutter/material.dart';

class InstructorSettings extends StatelessWidget {
  const InstructorSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Configuración Instructor", style: TextStyle(fontSize: 22)),
    );
  }
}
