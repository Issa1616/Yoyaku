import 'package:flutter/material.dart';

class OwnerClasses extends StatelessWidget {
  const OwnerClasses({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Mis Clases", style: TextStyle(fontSize: 22)),
    );
  }
}
