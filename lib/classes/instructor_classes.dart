import 'package:flutter/material.dart';

class InstructorClasses extends StatelessWidget {
  const InstructorClasses({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Mis Clases", style: TextStyle(fontSize: 22)),
    );
  }
}
