import 'package:flutter/material.dart';

class MyBussness extends StatelessWidget {
  const MyBussness({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Mi negocio", style: TextStyle(fontSize: 22)),
    );
  }
}
